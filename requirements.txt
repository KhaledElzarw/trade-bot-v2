# Runtime dependencies.
# Pinned to deliberate compatible ranges; see docs/architecture.md.

# API / dashboard server
fastapi>=0.115,<1.0
uvicorn>=0.32,<1.0

# Typed models and validation (LLM response schemas, API contracts)
pydantic>=2.7,<3.0

# HTTP client for the DataBroker and llama.cpp adapters (explicit timeouts,
# connection pooling). Replaces `requests` in the new stack.
httpx>=0.27,<1.0

# Legacy stack (retained until the singleton engine is removed in Phase 13).
# Pins raised to clear pip-audit findings: PYSEC-2026-2270 (python-dotenv),
# PYSEC-2026-1872 and PYSEC-2026-2275 (requests).
python-dotenv>=1.2.2,<2.0
requests>=2.33.0,<3.0
